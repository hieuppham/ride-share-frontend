import { DateInArrayPipe } from './date-in-array.pipe';

describe('DateInArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
