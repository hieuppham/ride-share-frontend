import { UpdateStatusPipe } from './update-status.pipe';

describe('UpdateStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
