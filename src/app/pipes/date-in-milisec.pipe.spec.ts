import { DateInMilisecPipe } from './date-in-milisec.pipe';

describe('DateInMilisecPipe', () => {
  it('create an instance', () => {
    const pipe = new DateInMilisecPipe();
    expect(pipe).toBeTruthy();
  });
});
